function descendingOrder() {
   let num= 209482;
   let arr_num = array.from(num + "");
   let arr_num = num_arr.map(e=> parseInt(e));
   arr_num.sort();
   arr_num.reverse();
   arr_num.sort(a,b) => a<b);
   let str_arr = arr_num.join;

}
